import { L } from '../../../../utils/lang';

const typeList = [
  { label: L('windows'), value: 'windows' },
  { label: L('linux'), value: 'linux' },
  { label: L('private cloud'), value: 'private cloud' }
];

export default typeList;
